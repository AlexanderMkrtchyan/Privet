/// Hint for the browser share picker (cannot force — security policy).
enum DisplayShareSurface {
  /// Whole monitor / desktop.
  monitor,

  /// A single application window.
  window,

  /// A browser tab.
  browser,
}
